# CoStage Scraping Backend — environment variables
# Copy this to .env and fill in your values
#
# No API key is required — product extraction is free and rule-based
# (JSON-LD parsing + heuristic DOM scraping).

# Optional — defaults to 3001
PORT=3001
